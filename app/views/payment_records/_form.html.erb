<%= form_with(model: payment_record) do |form| %>
  <% if payment_record.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(payment_record.errors.count, "error") %> prohibited this payment_record from being saved:</h2>

      <ul>
      <% payment_record.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :name %>
    <%= form.text_field :name %>
  </div>

  <div class="field">
    <%= form.label :money %>
    <%= form.text_field :money %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
